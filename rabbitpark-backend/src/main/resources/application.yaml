spring:
  application:
    name: demo
  datasource:
    url: jdbc:postgresql://localhost:5433/rabbitpark
    username: rabbit
    password: rabbit123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect